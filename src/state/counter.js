const initialState = 0

export default (state = initialState, action) => {
  return state
}

// export const reducerName = (state = , action) => {
//   switch (action.type) {
//     case 'ACTION_TYPE':
//       return 
//     default:
//       return state
//   }
// }